import java.util.*;

    public class HOFuncs {
        public static void main (String[] args){
            List carNames = Arrays.asList("lamborghini", "ferrari", "porsche", "tesla");
           // Collections.sort(carNames, (first, second) -> first.length()-second.length());
            System.out.println(carNames);
        }
    }